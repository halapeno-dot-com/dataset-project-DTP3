{
    "builds": [
        {
            "src": "api/astronautwebsite.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "(.*)",
            "dest": "api/astronautwebsite.py"
        },
        {
            "src": "api/(.*)",
            "headers": {"Access-Control-Allow-Origin": "*"},
            "continue": true
        }
    ]
}